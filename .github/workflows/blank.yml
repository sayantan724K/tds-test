name: test-workflow

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 22f3002449@ds.study.iitm.ac.in
        run: echo "Hello, world!"
